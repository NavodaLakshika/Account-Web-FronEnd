import React from 'react';
import MasterSubModal from './MasterSubModal/MasterSubModal';

const MasterFileModal = ({ isOpen, onClose }) => {
    return <MasterSubModal isOpen={isOpen} onClose={onClose} />;
};

export default MasterFileModal;
